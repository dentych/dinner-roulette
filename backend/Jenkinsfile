pipeline {
    agent any

    stages {
        stage("Build backend docker image") {
            steps {
                dir("backend") {
                    script {
                        def img = docker.build("dinnerdash/backend")
                        img.push()
                    }
                }
            }
        }
    }
}
