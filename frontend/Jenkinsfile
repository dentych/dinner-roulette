pipeline {
    agent {
        docker "node:alpine"
    }

    stages {
        stage("Build") {
            steps {
                def currentFolder = pwd()
                
                echo currentFolder
            }
        }
    }
}
