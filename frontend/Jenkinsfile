pipeline {
    agent any

    stages {
        stage("Build docker image") {
            steps {
                echo "Building frontend docker image"
                dir("frontend") {
                    script {
                        def img = docker.build("dinnerdash/frontend")
                    }
                }
            }
        }
    }
}
